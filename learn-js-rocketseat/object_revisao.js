// Object

const person = {
    name: 'John',
    age: 30,
    weight: 88.6,
    isAdmin: true
}

// impressão total
console.log(person)

// impressão de um item, basta adicionar um ponto, após o objeto
console.log(person.name)

// usando irterpolação
console.log(`${person.name} tem ${person.age} anos`)